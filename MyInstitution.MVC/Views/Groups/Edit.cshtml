@model MyInstitution.MVC.Models.GroupModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Group</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Group.Id" />
            <div class="form-group">
                <label asp-for="Group.Name" class="control-label"></label>
                <input asp-for="Group.Name" class="form-control" />
                <span asp-validation-for="Group.Name" class="text-danger"></span>
            </div>
            @*<div class="form-group">
                <label asp-for="Group.GroupLeaderEmployeeId" class="control-label"></label>
                <input asp-for="Group.GroupLeaderEmployeeId" class="form-control" />
                <span asp-validation-for="Group.GroupLeaderEmployeeId" class="text-danger"></span>
            </div>*@

            @*@Html.DropDownList("Employees", new SelectList(Model.Employees, "Id", "Forename"))*@

            <select class="custom-select" asp-for="Group.GroupLeaderEmployeeId" asp-items="Model.Employees">
                <option value="">All</option>
            </select>

            @*<div class="form-group">
                <label asp-for="Employees.te.Department" class="control-label"></label>
                <select asp-for="Employees.DepartmentID" class="form-control"
                        asp-items="@Model.DepartmentNameSL">
                    <option value="">-- Select Department --</option>
                </select>
                <span asp-validation-for="Course.DepartmentID" class="text-danger" />
            </div>*@

            @*@Html.DropDownListFor(model => model.ClientID, (SelectList)ViewBag.ClientsList, new { @class = "form-control" })*@

            <div class="form-group">
                <label asp-for="Group.Image" class="control-label"></label>
                <input asp-for="Group.Image" class="form-control" />
                <span asp-validation-for="Group.Image" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
